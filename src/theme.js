import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6CDAAF',
    },
    secondary: {
      main: '#fff',
    },
    background: {
      default: '#131313',
    },
  },
});

export default theme;