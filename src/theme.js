import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6CDAAF',
    },
    secondary: {
      main: '#6CDAAF',
    },
    background: {
      default: '#131313',
    },
  },
});

export default theme;